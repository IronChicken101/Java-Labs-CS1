
/**
 * Write a description of class HotPepperPlantRunner here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class HotPepperPlantRunner
{
    public HotPepperPlantRunner()
    {
        // initialise instance variables
        System.out.println("Grow<g>, Water<w>, fertilizer<f>, Pick<p>, Status<s>, Quit<q>");
        
        //if(answer = "q")
        {
          System.out.println("Goodbye");
          //else if(answer = "g") 
          {
              
          }
          //else if(answer = "w")
            {
                
            }
          //else if(answer = "f")
            {
                    
            }
          //else if(answer = "p")
            {
                        
            }
          //else if(answer = "s")
            {
                         
            }
        }
    }
}
